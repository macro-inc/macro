# Properties Service Database Client

Database client for accessing properties tables in macrodb.

The properties tables (property_definitions, property_options, entity_properties) are stored in the main macrodb database.

The service entrypoint is the `properties_service` crate.

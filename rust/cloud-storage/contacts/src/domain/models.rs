/// Generic graph data structures (vertices and edges).
pub mod graph;
/// SQS message types for the contacts queue.
pub mod messages;
/// User-specific graph abstractions (groups, connections).
pub mod user;

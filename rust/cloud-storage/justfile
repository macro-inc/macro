import './database.just'

mod sqlx './sqlx.just'

check:
    RUSTFLAGS=-Dwarnings RUSTDOCFLAGS=-Dwarnings CARGO_TERM_COLOR=always SQLX_OFFLINE=true cargo check

clippy:
    RUSTFLAGS=-Dwarnings RUSTDOCFLAGS=-Dwarnings CARGO_TERM_COLOR=always SQLX_OFFLINE=true cargo clippy

# This can be used in any crate that uses macrodb under the hood
[no-cd]
prepare_db:
    just sqlx::prepare_db {{ DATABASE_URL }}

format:
    SQLX_OFFLINE=true cargo fmt

clean:
    cargo clean
    git restore target/lambda

build:
    SQLX_OFFLINE=true cargo build

# Dry-run markdown lifecycle backfill against dev DB/sync-service.
# Extra script flags can be passed directly after the recipe name, for example:
# just backfill_markdown_content_location_dev --limit 100 --batch-size 25
backfill_markdown_content_location_dev *FLAGS:
    #!/usr/bin/env bash
    set -euo pipefail
    DATABASE_URL="$(./scripts/rds_database_url.sh macro-db-dev macrodb-password-dev macrodb)"
    SYNC_SERVICE_AUTH_KEY="$(aws secretsmanager get-secret-value --secret-id sync-service-key-dev --query SecretString --output text)"
    INTERNAL_API_SECRET_KEY="$(aws secretsmanager get-secret-value --secret-id document-storage-service-auth-key-dev --query SecretString --output text)"
    eval "$(aws configure export-credentials --format env)"
    SYNC_SERVICE_URL="https://sync-service-dev3.macroverse.workers.dev"
    LEXICAL_SERVICE_URL="https://lexical-service-dev.macroverse.workers.dev"
    DOCUMENT_STORAGE_BUCKET="doc-storage-dev"
    RUST_LOG="${RUST_LOG:-info}" DATABASE_URL="$DATABASE_URL" SYNC_SERVICE_AUTH_KEY="$SYNC_SERVICE_AUTH_KEY" INTERNAL_API_SECRET_KEY="$INTERNAL_API_SECRET_KEY" SYNC_SERVICE_URL="$SYNC_SERVICE_URL" LEXICAL_SERVICE_URL="$LEXICAL_SERVICE_URL" DOCUMENT_STORAGE_BUCKET="$DOCUMENT_STORAGE_BUCKET" \
        cargo run -p documents --bin backfill_markdown_content_location -- {{ FLAGS }}

# Apply markdown lifecycle backfill against dev DB/sync-service.
# Extra script flags can be passed directly after the recipe name, for example:
# just backfill_markdown_content_location_dev_apply --limit 100 --batch-size 25
backfill_markdown_content_location_dev_apply *FLAGS:
    #!/usr/bin/env bash
    set -euo pipefail
    echo -e "\033[33mWARNING: this will update dev Document.contentState/contentLocation for markdown documents found in sync-service.\033[0m"
    echo -n "Continue? [y/N] "
    read -r response
    if [[ ! "$response" =~ ^[Yy]$ ]]; then
        echo "Backfill cancelled."
        exit 0
    fi
    DATABASE_URL="$(./scripts/rds_database_url.sh macro-db-dev macrodb-password-dev macrodb)"
    SYNC_SERVICE_AUTH_KEY="$(aws secretsmanager get-secret-value --secret-id sync-service-key-dev --query SecretString --output text)"
    INTERNAL_API_SECRET_KEY="$(aws secretsmanager get-secret-value --secret-id document-storage-service-auth-key-dev --query SecretString --output text)"
    eval "$(aws configure export-credentials --format env)"
    SYNC_SERVICE_URL="https://sync-service-dev3.macroverse.workers.dev"
    LEXICAL_SERVICE_URL="https://lexical-service-dev.macroverse.workers.dev"
    DOCUMENT_STORAGE_BUCKET="doc-storage-dev"
    RUST_LOG="${RUST_LOG:-info}" DATABASE_URL="$DATABASE_URL" SYNC_SERVICE_AUTH_KEY="$SYNC_SERVICE_AUTH_KEY" INTERNAL_API_SECRET_KEY="$INTERNAL_API_SECRET_KEY" SYNC_SERVICE_URL="$SYNC_SERVICE_URL" LEXICAL_SERVICE_URL="$LEXICAL_SERVICE_URL" DOCUMENT_STORAGE_BUCKET="$DOCUMENT_STORAGE_BUCKET" \
        cargo run -p documents --bin backfill_markdown_content_location -- --apply {{ FLAGS }}

# Build dev service Docker images to pre-warm the cache used by root `run_local`.
build_dev_service_images:
  docker build -f Dockerfile.dev --target services_bundle -t macro-local-rust-services:dev .
  docker build -f Dockerfile.search_processing_service.dev --target search_processing_service -t macro-local-search-processing-service:dev .

# Build all lambda functions
build_lambdas compiler="":
    ulimit -n 10240 && just docx_unzip_handler/build {{compiler}}
    ulimit -n 10240 && just delete_chat_handler/build {{compiler}}
    ulimit -n 10240 && just organization_retention_handler/build {{compiler}}
    ulimit -n 10240 && just organization_retention_trigger/build {{compiler}}
    ulimit -n 10240 && just upload_extractor_lambda_trigger/build {{compiler}}
    ulimit -n 10240 && just upload_extractor_lambda_handler/build {{compiler}}
    ulimit -n 10240 && just worker_trigger/build {{compiler}}
    ulimit -n 10240 && just dataloss_prevention_handler/build {{compiler}}
    ulimit -n 10240 && just search_upload_handler/build {{compiler}}
    ulimit -n 10240 && just document_text_extractor/build {{compiler}}
    ulimit -n 10240 && just document_upload_finalizer_handler/build {{compiler}}
    ulimit -n 10240 && just email_suppression_handler/build {{compiler}}
    ulimit -n 10240 && just deleted_item_poller/build {{compiler}}
    ulimit -n 10240 && just email_refresh_handler/build {{compiler}}
    ulimit -n 10240 && just user_link_cleanup_handler/build {{compiler}}
    ulimit -n 10240 && just email_scheduled_handler/build {{compiler}}
    ulimit -n 10240 && just email_sfs_delete_handler/build {{compiler}}
    ulimit -n 10240 && just image_optimizer/build {{compiler}}

# TESTING

# Copies over necessary .env files for sqlx tests
setup_test_envs:
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./contacts/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./delete_chat_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./integration_tests/test_pdf_modification_data_deserialize/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./email_sfs_delete_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./scheduled_action/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./notification_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./notification/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./properties_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./macro_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./email_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./email_refresh_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./comms_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./comms/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./document_cognition_service/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./document_text_extractor/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./organization_retention_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./organization_retention_trigger/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./organization_retention_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./sha_cleanup_worker/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./roles_and_permissions/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./teams/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./frecency/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./soup/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./email/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./system_properties/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./properties/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./name_search/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./email_scheduled_handler/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./saved_views/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./channels/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./comms_service/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./documents/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./stream/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./macro_project_utils/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./entity_access/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./github/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./referral/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./chat/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./memory/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./call/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./backfill_entity_access/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./entity_access_management/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./mobile_welcome_email_db_client/.env
    echo "DATABASE_URL={{ DATABASE_URL }}" >> ./entity_access_db_utils/.env

setup_macrodb:
    just macro_db_client/create_db
    just macro_db_client/migrate_db

initialize_dbs: setup_macrodb

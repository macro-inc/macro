[package]
edition = "2024"
name = "sync_service"
publish = false
version = "0.1.0"

[features]
default = []
inbound = ["dep:axum"]

[dependencies]
axum = { workspace = true, optional = true }
serde = { workspace = true }

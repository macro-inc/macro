# graphql_soup

This crate provides the soup-domain GraphQL wrapper types. The composed
schema (root query, viewer, SDL export) lives in `complete_graph` — see
`../complete_graph/AGENTS.md` for the schema conventions (presence-of-`id`
normalized-cache rules and the viewer pattern), which apply to every type
defined here.

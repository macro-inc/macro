[package]
edition = "2024"
name = "sqs_client"
publish = false
version = "0.1.0"

[features]
chat = []
contacts = ["model"]
convert = ["model"]
default = [
  "chat",
  "contacts",
  "convert",
  "document",
  "document_text_extractor",
  "email",
  "gmail",
  "organization_retention",
  "search",
  "sfs_delete",
  "sfs_uploader",
  "upload_extractor",
]
document = []
document_text_extractor = []
email = ["uuid", "models_email", "dep:email"]
gmail = ["model", "uuid"]
organization_retention = []
search = ["chrono", "model", "models_opensearch", "uuid", "futures"]
sfs_delete = []
sfs_uploader = []
upload_extractor = []

[dependencies]
anyhow = { workspace = true }
aws-sdk-sqs = { workspace = true }
chrono = { workspace = true, optional = true }
email = { path = "../email", features = ["ports"], optional = true }
model = { path = "../model", optional = true }
models_email = { path = "../models_email", optional = true }
serde = { workspace = true }
serde_json = { workspace = true }
strum = { workspace = true, features = ["derive"] }
tracing = { workspace = true }
uuid = { workspace = true, optional = true }
macro_user_id = { path = "../macro_user_id", optional = true }
models_opensearch = { path = "../models_opensearch", optional = true }
futures = { workspace = true, optional = true }

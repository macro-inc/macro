[package]
description = "Shared guppy PackageGraph builder for the graph-based xtask commands"
edition = "2024"
name = "xtask-graph"
publish = false
version = "0.1.0"

[dependencies]
anyhow = { workspace = true }
guppy = { workspace = true }
xtask-paths = { path = "../xtask-paths" }

# Sha Ref Count Resetter

This assumes that the redis cluster has been emptied of all sha data.

This is a simple program that will reset the sha ref count in redis cli should it ever get corrupted or out of sync.

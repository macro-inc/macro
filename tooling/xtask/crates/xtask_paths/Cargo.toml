[package]
description = "Shared path discovery for the xtask command crates (workspace + repo roots)"
edition = "2024"
name = "xtask_paths"
publish = false
version = "0.1.0"

[dependencies]
glob = { workspace = true }

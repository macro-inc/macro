# Calendar invitation demo

[Download the 69-second MP4](calendar-invitations.mp4).

Recorded with `just run_local --no-doppler --instance calendar-invites --with-chrome`. Uses synthetic invitations and real local email/calendar read APIs. Google RSVP writes and the grant health check are simulated; authenticated Google writes are not verified.

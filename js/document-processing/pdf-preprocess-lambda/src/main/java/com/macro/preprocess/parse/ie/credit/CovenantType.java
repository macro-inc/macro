package com.macro.preprocess.parse.ie.credit;

import com.macro.preprocess.parse.ie.ClauseType;

public enum CovenantType implements ClauseType {
	FINANCIAL, RATES, LIENS, INDEBTEDNESS, RESTRICTED_PAYMENTS, OTHER;
}

{
	"name": "ai-editing-worker",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"prebuild": "bun scripts/generate-sandbox.ts",
		"dev": "bun scripts/generate-sandbox.ts && wrangler dev --env local",
		"deploy": "bun scripts/generate-sandbox.ts && wrangler deploy",
		"deploy:dev": "bun scripts/generate-sandbox.ts && wrangler deploy --env dev",
		"deploy:prod": "bun scripts/generate-sandbox.ts && wrangler deploy --env prod",
		"type-check": "tsc --noEmit --project tsconfig.json"
	},
	"dependencies": {
		"@ai-sdk/anthropic": "^3.0.85",
		"@ai-sdk/cerebras": "^2.0.57",
		"@ai-sdk/openai": "^3.0.73",
		"@hono/zod-validator": "^0.8.0",
		"ai": "^6.0.207",
		"envsafe": "^2.0.3",
		"hono": "^4.6.20",
		"quickjs-emscripten": "^0.31.0",
		"yargs": "^18.0.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^25.9.3",
		"@types/yargs": "^17.0.35",
		"bun-types": "^1.3.14",
		"wrangler": "^4.103.0"
	}
}

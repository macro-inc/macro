diff --git a/.bun-tag-14e38afd22c82559 b/.bun-tag-14e38afd22c82559
new file mode 100644
index 000000000..e69de29bb
diff --git a/build/modern/infiniteQueryBehavior.cjs b/build/modern/infiniteQueryBehavior.cjs
index 1891efa1b..1f63326ea 100644
--- a/build/modern/infiniteQueryBehavior.cjs
+++ b/build/modern/infiniteQueryBehavior.cjs
@@ -26,6 +26,30 @@ __export(infiniteQueryBehavior_exports, {
 });
 module.exports = __toCommonJS(infiniteQueryBehavior_exports);
 var import_utils = require("./utils.cjs");
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function infiniteQueryBehavior(pages) {
   return {
     onFetch: (context, query) => {
@@ -54,10 +78,26 @@ function infiniteQueryBehavior(pages) {
         };
         const queryFn = (0, import_utils.ensureQueryFn)(context.options, context.fetchOptions);
         const fetchPage = async (data, param, previous) => {
+          const startedAtTotal = nowMs();
+          const basePayload = {
+            queryHash: query.queryHash,
+            queryKey: context.queryKey,
+            direction: previous ? "backward" : "forward",
+            pageParam: param
+          };
           if (cancelled) {
             return Promise.reject();
           }
           if (param == null && data.pages.length) {
+            logQueryProfile(
+              "query-core.infinite.mergePage.skipped",
+              nowMs() - startedAtTotal,
+              {
+                ...basePayload,
+                reason: "missing-page-param",
+                pagesBefore: data.pages.length
+              }
+            );
             return Promise.resolve(data);
           }
           const createQueryFnContext = () => {
@@ -72,12 +112,34 @@ function infiniteQueryBehavior(pages) {
             return queryFnContext2;
           };
           const queryFnContext = createQueryFnContext();
+          const startedAtQueryFn = nowMs();
           const page = await queryFn(queryFnContext);
+          logQueryProfile("query-core.infinite.queryFn", nowMs() - startedAtQueryFn, {
+            ...basePayload,
+            pagesBefore: data.pages.length
+          });
           const { maxPages } = context.options;
           const addTo = previous ? import_utils.addToStart : import_utils.addToEnd;
+          const startedAtAppend = nowMs();
+          const nextPages = addTo(data.pages, page, maxPages);
+          const nextPageParams = addTo(data.pageParams, param, maxPages);
+          logQueryProfile("query-core.infinite.appendOnly", nowMs() - startedAtAppend, {
+            ...basePayload,
+            pagesBefore: data.pages.length,
+            pagesAfter: nextPages.length,
+            pageParamsAfter: nextPageParams.length,
+            maxPages
+          });
+          logQueryProfile("query-core.infinite.mergePage.total", nowMs() - startedAtTotal, {
+            ...basePayload,
+            pagesBefore: data.pages.length,
+            pagesAfter: nextPages.length,
+            pageParamsAfter: nextPageParams.length,
+            maxPages
+          });
           return {
-            pages: addTo(data.pages, page, maxPages),
-            pageParams: addTo(data.pageParams, param, maxPages)
+            pages: nextPages,
+            pageParams: nextPageParams
           };
         };
         if (direction && oldPages.length) {
diff --git a/build/modern/infiniteQueryBehavior.js b/build/modern/infiniteQueryBehavior.js
index 85e614f81..918438a98 100644
--- a/build/modern/infiniteQueryBehavior.js
+++ b/build/modern/infiniteQueryBehavior.js
@@ -1,5 +1,29 @@
 // src/infiniteQueryBehavior.ts
 import { addToEnd, addToStart, ensureQueryFn } from "./utils.js";
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function infiniteQueryBehavior(pages) {
   return {
     onFetch: (context, query) => {
@@ -28,10 +52,26 @@ function infiniteQueryBehavior(pages) {
         };
         const queryFn = ensureQueryFn(context.options, context.fetchOptions);
         const fetchPage = async (data, param, previous) => {
+          const startedAtTotal = nowMs();
+          const basePayload = {
+            queryHash: query.queryHash,
+            queryKey: context.queryKey,
+            direction: previous ? "backward" : "forward",
+            pageParam: param
+          };
           if (cancelled) {
             return Promise.reject();
           }
           if (param == null && data.pages.length) {
+            logQueryProfile(
+              "query-core.infinite.mergePage.skipped",
+              nowMs() - startedAtTotal,
+              {
+                ...basePayload,
+                reason: "missing-page-param",
+                pagesBefore: data.pages.length
+              }
+            );
             return Promise.resolve(data);
           }
           const createQueryFnContext = () => {
@@ -46,12 +86,34 @@ function infiniteQueryBehavior(pages) {
             return queryFnContext2;
           };
           const queryFnContext = createQueryFnContext();
+          const startedAtQueryFn = nowMs();
           const page = await queryFn(queryFnContext);
+          logQueryProfile("query-core.infinite.queryFn", nowMs() - startedAtQueryFn, {
+            ...basePayload,
+            pagesBefore: data.pages.length
+          });
           const { maxPages } = context.options;
           const addTo = previous ? addToStart : addToEnd;
+          const startedAtAppend = nowMs();
+          const nextPages = addTo(data.pages, page, maxPages);
+          const nextPageParams = addTo(data.pageParams, param, maxPages);
+          logQueryProfile("query-core.infinite.appendOnly", nowMs() - startedAtAppend, {
+            ...basePayload,
+            pagesBefore: data.pages.length,
+            pagesAfter: nextPages.length,
+            pageParamsAfter: nextPageParams.length,
+            maxPages
+          });
+          logQueryProfile("query-core.infinite.mergePage.total", nowMs() - startedAtTotal, {
+            ...basePayload,
+            pagesBefore: data.pages.length,
+            pagesAfter: nextPages.length,
+            pageParamsAfter: nextPageParams.length,
+            maxPages
+          });
           return {
-            pages: addTo(data.pages, page, maxPages),
-            pageParams: addTo(data.pageParams, param, maxPages)
+            pages: nextPages,
+            pageParams: nextPageParams
           };
         };
         if (direction && oldPages.length) {
diff --git a/build/modern/query.cjs b/build/modern/query.cjs
index 7de2cc59b..6c247309d 100644
--- a/build/modern/query.cjs
+++ b/build/modern/query.cjs
@@ -28,6 +28,35 @@ var import_utils = require("./utils.cjs");
 var import_notifyManager = require("./notifyManager.cjs");
 var import_retryer = require("./retryer.cjs");
 var import_removable = require("./removable.cjs");
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 var Query = class extends import_removable.Removable {
   #initialState;
   #revertState;
@@ -76,6 +105,8 @@ var Query = class extends import_removable.Removable {
     }
   }
   setData(newData, options) {
+    const startedAt = nowMs();
+    const previousData = this.state.data;
     const data = (0, import_utils.replaceData)(this.state.data, newData, this.options);
     this.#dispatch({
       data,
@@ -83,6 +114,13 @@ var Query = class extends import_removable.Removable {
       dataUpdatedAt: options?.updatedAt,
       manual: options?.manual
     });
+    logQueryProfile("query-core.query.setData", nowMs() - startedAt, {
+      queryHash: this.queryHash,
+      queryKey: this.queryKey,
+      manual: options?.manual === true,
+      previousPages: countInfinitePages(previousData),
+      nextPages: countInfinitePages(data)
+    });
     return data;
   }
   setState(state, setStateOptions) {
@@ -441,5 +479,4 @@ function getDefaultState(options) {
 0 && (module.exports = {
   Query,
   fetchState
-});
-//# sourceMappingURL=query.cjs.map
\ No newline at end of file
+});
\ No newline at end of file
diff --git a/build/modern/query.js b/build/modern/query.js
index b9376ecf5..6d69c3eb4 100644
--- a/build/modern/query.js
+++ b/build/modern/query.js
@@ -11,6 +11,35 @@ import {
 import { notifyManager } from "./notifyManager.js";
 import { CancelledError, canFetch, createRetryer } from "./retryer.js";
 import { Removable } from "./removable.js";
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 var Query = class extends Removable {
   #initialState;
   #revertState;
@@ -59,6 +88,8 @@ var Query = class extends Removable {
     }
   }
   setData(newData, options) {
+    const startedAt = nowMs();
+    const previousData = this.state.data;
     const data = replaceData(this.state.data, newData, this.options);
     this.#dispatch({
       data,
@@ -66,6 +97,13 @@ var Query = class extends Removable {
       dataUpdatedAt: options?.updatedAt,
       manual: options?.manual
     });
+    logQueryProfile("query-core.query.setData", nowMs() - startedAt, {
+      queryHash: this.queryHash,
+      queryKey: this.queryKey,
+      manual: options?.manual === true,
+      previousPages: countInfinitePages(previousData),
+      nextPages: countInfinitePages(data)
+    });
     return data;
   }
   setState(state, setStateOptions) {
@@ -423,5 +461,4 @@ function getDefaultState(options) {
 export {
   Query,
   fetchState
-};
-//# sourceMappingURL=query.js.map
\ No newline at end of file
+};
\ No newline at end of file
diff --git a/build/modern/queryClient.cjs b/build/modern/queryClient.cjs
index 37e298a58..f5334566a 100644
--- a/build/modern/queryClient.cjs
+++ b/build/modern/queryClient.cjs
@@ -30,6 +30,35 @@ var import_focusManager = require("./focusManager.cjs");
 var import_onlineManager = require("./onlineManager.cjs");
 var import_notifyManager = require("./notifyManager.cjs");
 var import_infiniteQueryBehavior = require("./infiniteQueryBehavior.cjs");
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 var QueryClient = class {
   #queryCache;
   #mutationCache;
@@ -107,6 +136,7 @@ var QueryClient = class {
     });
   }
   setQueryData(queryKey, updater, options) {
+    const startedAt = nowMs();
     const defaultedOptions = this.defaultQueryOptions({ queryKey });
     const query = this.#queryCache.get(
       defaultedOptions.queryHash
@@ -114,17 +144,36 @@ var QueryClient = class {
     const prevData = query?.state.data;
     const data = (0, import_utils.functionalUpdate)(updater, prevData);
     if (data === void 0) {
+      logQueryProfile("query-core.setQueryData", nowMs() - startedAt, {
+        queryHash: defaultedOptions.queryHash,
+        queryKey,
+        skipped: true,
+        previousPages: countInfinitePages(prevData)
+      });
       return void 0;
     }
-    return this.#queryCache.build(this, defaultedOptions).setData(data, { ...options, manual: true });
+    const result = this.#queryCache.build(this, defaultedOptions).setData(data, { ...options, manual: true });
+    logQueryProfile("query-core.setQueryData", nowMs() - startedAt, {
+      queryHash: defaultedOptions.queryHash,
+      queryKey,
+      previousPages: countInfinitePages(prevData),
+      nextPages: countInfinitePages(data)
+    });
+    return result;
   }
   setQueriesData(filters, updater, options) {
-    return import_notifyManager.notifyManager.batch(
+    const startedAt = nowMs();
+    const result = import_notifyManager.notifyManager.batch(
       () => this.#queryCache.findAll(filters).map(({ queryKey }) => [
         queryKey,
         this.setQueryData(queryKey, updater, options)
       ])
     );
+    logQueryProfile("query-core.setQueriesData", nowMs() - startedAt, {
+      queryCount: result.length,
+      queryKey: filters?.queryKey
+    });
+    return result;
   }
   getQueryState(queryKey) {
     const options = this.defaultQueryOptions({ queryKey });
@@ -319,4 +368,4 @@ var QueryClient = class {
 0 && (module.exports = {
   QueryClient
 });
-//# sourceMappingURL=queryClient.cjs.map
\ No newline at end of file
+//# sourceMappingURL=queryClient.cjs.map
diff --git a/build/modern/queryClient.js b/build/modern/queryClient.js
index 64a5a5f3a..64d155980 100644
--- a/build/modern/queryClient.js
+++ b/build/modern/queryClient.js
@@ -14,6 +14,35 @@ import { focusManager } from "./focusManager.js";
 import { onlineManager } from "./onlineManager.js";
 import { notifyManager } from "./notifyManager.js";
 import { infiniteQueryBehavior } from "./infiniteQueryBehavior.js";
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 var QueryClient = class {
   #queryCache;
   #mutationCache;
@@ -91,6 +120,7 @@ var QueryClient = class {
     });
   }
   setQueryData(queryKey, updater, options) {
+    const startedAt = nowMs();
     const defaultedOptions = this.defaultQueryOptions({ queryKey });
     const query = this.#queryCache.get(
       defaultedOptions.queryHash
@@ -98,17 +128,36 @@ var QueryClient = class {
     const prevData = query?.state.data;
     const data = functionalUpdate(updater, prevData);
     if (data === void 0) {
+      logQueryProfile("query-core.setQueryData", nowMs() - startedAt, {
+        queryHash: defaultedOptions.queryHash,
+        queryKey,
+        skipped: true,
+        previousPages: countInfinitePages(prevData)
+      });
       return void 0;
     }
-    return this.#queryCache.build(this, defaultedOptions).setData(data, { ...options, manual: true });
+    const result = this.#queryCache.build(this, defaultedOptions).setData(data, { ...options, manual: true });
+    logQueryProfile("query-core.setQueryData", nowMs() - startedAt, {
+      queryHash: defaultedOptions.queryHash,
+      queryKey,
+      previousPages: countInfinitePages(prevData),
+      nextPages: countInfinitePages(data)
+    });
+    return result;
   }
   setQueriesData(filters, updater, options) {
-    return notifyManager.batch(
+    const startedAt = nowMs();
+    const result = notifyManager.batch(
       () => this.#queryCache.findAll(filters).map(({ queryKey }) => [
         queryKey,
         this.setQueryData(queryKey, updater, options)
       ])
     );
+    logQueryProfile("query-core.setQueriesData", nowMs() - startedAt, {
+      queryCount: result.length,
+      queryKey: filters?.queryKey
+    });
+    return result;
   }
   getQueryState(queryKey) {
     const options = this.defaultQueryOptions({ queryKey });
@@ -302,4 +351,4 @@ var QueryClient = class {
 export {
   QueryClient
 };
-//# sourceMappingURL=queryClient.js.map
\ No newline at end of file
+//# sourceMappingURL=queryClient.js.map

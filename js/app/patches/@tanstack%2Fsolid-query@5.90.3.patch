diff --git a/.bun-tag-416aefb059b66cc5 b/.bun-tag-416aefb059b66cc5
new file mode 100644
index 000000000..e69de29bb
diff --git a/build/dev.cjs b/build/dev.cjs
index d989a2917..b3171cc11 100644
--- a/build/dev.cjs
+++ b/build/dev.cjs
@@ -36,10 +36,53 @@ exports.useIsRestoring = () => solidJs.useContext(IsRestoringContext);
 exports.IsRestoringProvider = IsRestoringContext.Provider;
 
 // src/useBaseQuery.ts
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function reconcileFn(store$1, result, reconcileOption, queryHash) {
-  if (reconcileOption === false) return result;
+  const startedAt = nowMs();
+  if (reconcileOption === false) {
+    logQueryProfile("solid-query.reconcile.skipped", nowMs() - startedAt, {
+      queryHash,
+      previousPages: countInfinitePages(store$1.data),
+      nextPages: countInfinitePages(result.data)
+    });
+    return result;
+  }
   if (typeof reconcileOption === "function") {
+    const startedAt2 = nowMs();
     const newData2 = reconcileOption(store$1.data, result.data);
+    logQueryProfile("solid-query.reconcile.custom", nowMs() - startedAt2, {
+      queryHash,
+      previousPages: countInfinitePages(store$1.data),
+      nextPages: countInfinitePages(newData2)
+    });
     return { ...result, data: newData2 };
   }
   let data = result.data;
@@ -58,7 +101,14 @@ function reconcileFn(store$1, result, reconcileOption, queryHash) {
       }
     }
   }
+  const startedAt2 = nowMs();
   const newData = store.reconcile(data, { key: reconcileOption })(store$1.data);
+  logQueryProfile("solid-query.reconcile", nowMs() - startedAt2, {
+    queryHash,
+    reconcileOption,
+    previousPages: countInfinitePages(store$1.data),
+    nextPages: countInfinitePages(newData)
+  });
   return { ...result, data: newData };
 }
 var hydratableObserverResult = (query, result) => {
diff --git a/build/dev.js b/build/dev.js
index d1b3addc6..1a90a8884 100644
--- a/build/dev.js
+++ b/build/dev.js
@@ -35,10 +35,53 @@ var useIsRestoring = () => useContext(IsRestoringContext);
 var IsRestoringProvider = IsRestoringContext.Provider;
 
 // src/useBaseQuery.ts
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function reconcileFn(store, result, reconcileOption, queryHash) {
-  if (reconcileOption === false) return result;
+  const startedAt = nowMs();
+  if (reconcileOption === false) {
+    logQueryProfile("solid-query.reconcile.skipped", nowMs() - startedAt, {
+      queryHash,
+      previousPages: countInfinitePages(store.data),
+      nextPages: countInfinitePages(result.data)
+    });
+    return result;
+  }
   if (typeof reconcileOption === "function") {
+    const startedAt2 = nowMs();
     const newData2 = reconcileOption(store.data, result.data);
+    logQueryProfile("solid-query.reconcile.custom", nowMs() - startedAt2, {
+      queryHash,
+      previousPages: countInfinitePages(store.data),
+      nextPages: countInfinitePages(newData2)
+    });
     return { ...result, data: newData2 };
   }
   let data = result.data;
@@ -57,7 +100,14 @@ function reconcileFn(store, result, reconcileOption, queryHash) {
       }
     }
   }
+  const startedAt2 = nowMs();
   const newData = reconcile(data, { key: reconcileOption })(store.data);
+  logQueryProfile("solid-query.reconcile", nowMs() - startedAt2, {
+    queryHash,
+    reconcileOption,
+    previousPages: countInfinitePages(store.data),
+    nextPages: countInfinitePages(newData)
+  });
   return { ...result, data: newData };
 }
 var hydratableObserverResult = (query, result) => {
diff --git a/build/index.cjs b/build/index.cjs
index 07a070007..792b785f4 100644
--- a/build/index.cjs
+++ b/build/index.cjs
@@ -36,10 +36,53 @@ exports.useIsRestoring = () => solidJs.useContext(IsRestoringContext);
 exports.IsRestoringProvider = IsRestoringContext.Provider;
 
 // src/useBaseQuery.ts
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function reconcileFn(store$1, result, reconcileOption, queryHash) {
-  if (reconcileOption === false) return result;
+  const startedAt = nowMs();
+  if (reconcileOption === false) {
+    logQueryProfile("solid-query.reconcile.skipped", nowMs() - startedAt, {
+      queryHash,
+      previousPages: countInfinitePages(store$1.data),
+      nextPages: countInfinitePages(result.data)
+    });
+    return result;
+  }
   if (typeof reconcileOption === "function") {
+    const startedAt2 = nowMs();
     const newData2 = reconcileOption(store$1.data, result.data);
+    logQueryProfile("solid-query.reconcile.custom", nowMs() - startedAt2, {
+      queryHash,
+      previousPages: countInfinitePages(store$1.data),
+      nextPages: countInfinitePages(newData2)
+    });
     return { ...result, data: newData2 };
   }
   let data = result.data;
@@ -49,7 +92,14 @@ function reconcileFn(store$1, result, reconcileOption, queryHash) {
     } catch (error) {
     }
   }
+  const startedAt2 = nowMs();
   const newData = store.reconcile(data, { key: reconcileOption })(store$1.data);
+  logQueryProfile("solid-query.reconcile", nowMs() - startedAt2, {
+    queryHash,
+    reconcileOption,
+    previousPages: countInfinitePages(store$1.data),
+    nextPages: countInfinitePages(newData)
+  });
   return { ...result, data: newData };
 }
 var hydratableObserverResult = (query, result) => {
diff --git a/build/index.js b/build/index.js
index afe1550d6..afb575792 100644
--- a/build/index.js
+++ b/build/index.js
@@ -35,10 +35,53 @@ var useIsRestoring = () => useContext(IsRestoringContext);
 var IsRestoringProvider = IsRestoringContext.Provider;
 
 // src/useBaseQuery.ts
+var QUERY_PROFILE_GLOBAL_KEY = "__MACRO_QUERY_PROFILE__";
+var nowMs = () => {
+  if (typeof performance !== "undefined" && typeof performance.now === "function") {
+    return performance.now();
+  }
+  return Date.now();
+};
+var readQueryProfileConfig = () => {
+  const globalConfig = typeof globalThis === "undefined" ? void 0 : globalThis[QUERY_PROFILE_GLOBAL_KEY];
+  if (globalConfig === true) {
+    return { enabled: true, thresholdMs: 0 };
+  }
+  if (!globalConfig || typeof globalConfig !== "object") {
+    return { enabled: false, thresholdMs: 0 };
+  }
+  const enabled = "enabled" in globalConfig ? globalConfig.enabled !== false : true;
+  const thresholdMs = "thresholdMs" in globalConfig && typeof globalConfig.thresholdMs === "number" ? globalConfig.thresholdMs : 0;
+  return { enabled, thresholdMs };
+};
+var countInfinitePages = (data) => {
+  if (!data || typeof data !== "object" || !("pages" in data)) return void 0;
+  const pages = data.pages;
+  return Array.isArray(pages) ? pages.length : void 0;
+};
+var logQueryProfile = (event, duration, payload) => {
+  const config = readQueryProfileConfig();
+  if (!config.enabled || duration < config.thresholdMs) return;
+  console.info(`[query-profile] ${event} ${duration.toFixed(2)}ms`, payload);
+};
 function reconcileFn(store, result, reconcileOption, queryHash) {
-  if (reconcileOption === false) return result;
+  const startedAt = nowMs();
+  if (reconcileOption === false) {
+    logQueryProfile("solid-query.reconcile.skipped", nowMs() - startedAt, {
+      queryHash,
+      previousPages: countInfinitePages(store.data),
+      nextPages: countInfinitePages(result.data)
+    });
+    return result;
+  }
   if (typeof reconcileOption === "function") {
+    const startedAt2 = nowMs();
     const newData2 = reconcileOption(store.data, result.data);
+    logQueryProfile("solid-query.reconcile.custom", nowMs() - startedAt2, {
+      queryHash,
+      previousPages: countInfinitePages(store.data),
+      nextPages: countInfinitePages(newData2)
+    });
     return { ...result, data: newData2 };
   }
   let data = result.data;
@@ -48,7 +91,14 @@ function reconcileFn(store, result, reconcileOption, queryHash) {
     } catch (error) {
     }
   }
+  const startedAt2 = nowMs();
   const newData = reconcile(data, { key: reconcileOption })(store.data);
+  logQueryProfile("solid-query.reconcile", nowMs() - startedAt2, {
+    queryHash,
+    reconcileOption,
+    previousPages: countInfinitePages(store.data),
+    nextPages: countInfinitePages(newData)
+  });
   return { ...result, data: newData };
 }
 var hydratableObserverResult = (query, result) => {

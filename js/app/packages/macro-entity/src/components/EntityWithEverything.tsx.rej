diff a/js/app/packages/macro-entity/src/components/EntityWithEverything.tsx b/js/app/packages/macro-entity/src/components/EntityWithEverything.tsx	(rejected hunks)
@@ -260,11 +262,12 @@ export function EntityWithEverything<
     <div
       use:draggable
       use:droppable
-      class={`relative group py-[7px] px-2 ${ITEM_WRAPPER_CLASS()}`}
+      class={`everything-entity relative group py-[7px] px-2 ${ITEM_WRAPPER_CLASS()}`}
       classList={{
         'bg-hover': props.highlighted,
         bracket: props.selected,
       }}
+      data-checked={props.checked}
       onMouseOver={(e) => {
         if (!didCursorMove(e)) {
           return;

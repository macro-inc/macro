# Filesystem

App-local wrappers for browser File System Access APIs, OPFS persistence,
downloads, file filtering, and permission handling.

The implementation originated from MIT-licensed TypeScript boilerplate; the
preserved [`LICENSE`](./LICENSE) applies to the derived code in this directory.

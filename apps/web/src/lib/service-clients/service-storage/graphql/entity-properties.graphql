query EntityProperties($input: SoupInput!) {
  user {
    id
    soup(input: $input) {
      items {
        __typename
        id
        properties {
          ...SoupPropertyFields
        }
      }
    }
  }
}

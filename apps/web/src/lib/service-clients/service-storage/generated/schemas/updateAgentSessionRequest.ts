/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * document_storage_service
 * OpenAPI spec version: 0.1.0
 */

import type { SessionStatusDto } from './sessionStatusDto';
import type { UpdateAgentSessionRequestAcpSessionId } from './updateAgentSessionRequestAcpSessionId';
import type { UpdateAgentSessionRequestOriginatingMessageId } from './updateAgentSessionRequestOriginatingMessageId';
import type { UpdateAgentSessionRequestThreadId } from './updateAgentSessionRequestThreadId';

/**
 * Request body for replacing an agent session. This is full-resource `PUT`
semantics: fetch the session, modify it, and send the whole thing back.
`channelId` and `createdAt` are immutable; echo the values returned by the
get endpoint.
 */
export interface UpdateAgentSessionRequest {
  /** The ACP session id, if one exists. */
  acpSessionId?: UpdateAgentSessionRequestAcpSessionId;
  /** The bot running the agent. */
  botId: string;
  /** The session's dedicated channel. Immutable; echo the value returned
by the get endpoint. */
  channelId: string;
  /** When the session was created. Immutable; echo the value returned by
the get endpoint. */
  createdAt: string;
  /** Harness slug. */
  harness: string;
  /** Model slug. */
  model: string;
  /** The exact message that invoked the bot, if any. */
  originatingMessageId?: UpdateAgentSessionRequestOriginatingMessageId;
  /** The repository the session works with. */
  repoUrl: string;
  /** The session's status. */
  status: SessionStatusDto;
  /** The root message of the thread the session was created from, if any. */
  threadId?: UpdateAgentSessionRequestThreadId;
}

/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * document_storage_service
 * OpenAPI spec version: 0.1.0
 */
import type { HarnessId } from './harnessId';
import type { HarnessLastConnectedAt } from './harnessLastConnectedAt';
import type { HarnessOwner } from './harnessOwner';

/**
 * A registered user-run harness.

Clients deserialize this, so both derives are used.
 */
export interface Harness {
  /** Whether the daemon currently holds a runtime connection. */
  connected: boolean;
  /** Creation timestamp. */
  created_at: string;
  /** User that registered this harness. */
  created_by: string;
  /** Harness id. */
  id: HarnessId;
  /** Runtime kind. Currently always `macrod`. */
  kind: string;
  /** When the daemon last attached a runtime connection. */
  last_connected_at?: HarnessLastConnectedAt;
  /** Display name. */
  name: string;
  /** Owner. */
  owner: HarnessOwner;
  /** Update timestamp. */
  updated_at: string;
}

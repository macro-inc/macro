/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * authentication_service
 * OpenAPI spec version: 0.1.0
 */
import type { GtmInviteLinkConvertedAt } from './gtmInviteLinkConvertedAt';
import type { GtmInviteLinkFirstOpenedAt } from './gtmInviteLinkFirstOpenedAt';
import type { GtmInviteLinkNote } from './gtmInviteLinkNote';
import type { GtmInviteLinkRecipientEmail } from './gtmInviteLinkRecipientEmail';
import type { GtmInviteLinkRedeemedAt } from './gtmInviteLinkRedeemedAt';
import type { GtmInviteLinkRedeemedBy } from './gtmInviteLinkRedeemedBy';
import type { GtmInviteLinkRevokedAt } from './gtmInviteLinkRevokedAt';
import type { GtmInviteLinkStatus } from './gtmInviteLinkStatus';
import type { GtmInviteLinkStripeSubscriptionId } from './gtmInviteLinkStripeSubscriptionId';

/**
 * A link as shown on the staff dashboard.
 */
export interface GtmInviteLink {
  /** When the account started a paid subscription. */
  convertedAt?: GtmInviteLinkConvertedAt;
  /** When the link was created. */
  createdAt: string;
  /** The Macro user id of the staff member who created the link. */
  createdBy: string;
  /** When the link stops being openable. */
  expiresAt: string;
  /** The recipient's first name. */
  firstName: string;
  /** When the welcome page first loaded. */
  firstOpenedAt?: GtmInviteLinkFirstOpenedAt;
  /**
   * Free months the promotion grants.
   * @minimum 0
   */
  freeMonths: number;
  /** Primary key. */
  id: string;
  /** Internal note. */
  note?: GtmInviteLinkNote;
  /** How many times the welcome page loaded. */
  openCount: number;
  /** The Stripe promotion code applied at checkout. */
  promoCode: string;
  /** The recipient's email, when known. */
  recipientEmail?: GtmInviteLinkRecipientEmail;
  /** When that account redeemed the link. */
  redeemedAt?: GtmInviteLinkRedeemedAt;
  /** The Macro user id of the account that signed up through the link. */
  redeemedBy?: GtmInviteLinkRedeemedBy;
  /** When staff revoked the link. */
  revokedAt?: GtmInviteLinkRevokedAt;
  /** Lifecycle status. */
  status: GtmInviteLinkStatus;
  /** The Stripe subscription id of that subscription. */
  stripeSubscriptionId?: GtmInviteLinkStripeSubscriptionId;
  /** The secret to embed in the link URL. */
  token: string;
}

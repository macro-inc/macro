/**
 * The word an open turn shows while the agent has produced nothing to read.
 * Deliberately not "Thinking": a bare row has no reasoning behind it and the
 * turn may well be running a tool.
 */
export const WORKING_LABEL = 'Working...';

import './site-navigation.css';

/** Keep scrolling content soft at the viewport edges, beneath the navigation. */
export function PageVignette() {
  return <div class="site-page-vignette" aria-hidden="true" />;
}

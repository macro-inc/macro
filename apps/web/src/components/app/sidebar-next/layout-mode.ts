/**
 * Which SidebarNext arrangement renders while both exist.
 *
 * `'wide'` is the 220px sidebar with labelled rows and a footer button row;
 * `'rail'` is the always-narrow 60px column of 36px icon buttons. Flip this one
 * line to compare them — they share their navs, actions and item chrome, so
 * only the arrangement changes.
 */
export const SIDEBAR_NEXT_LAYOUT: 'wide' | 'rail' = 'rail';

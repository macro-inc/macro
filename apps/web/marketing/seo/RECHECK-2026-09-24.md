# SEO / AEO recheck — September 24, 2026

Rechecked the working tree on `codex/marketing-homepage` against the live
[sitemap](https://macro.com/sitemap.xml), all 30 sitemap pages, their internal
links, [robots.txt](https://macro.com/robots.txt), and a fresh production build.
This is a local artifact audit, not a verification of a deployed candidate CDN.

## September 25 pre-push verification

After the final layout, factual onboarding copy, static sidebar, email scrolling,
and flat-button updates, a fresh production build and SEO gate passed again:
30 preserved live routes, 626 internal links, and 716 asset references. Homepage
HTML is 45,725 bytes gzip and initial JavaScript is 392,647 bytes gzip, below the
400,000-byte budget. The sidebar's illustrative links were intentionally removed;
the corresponding pages and section anchors remain available.

All 143 targeted tests across setup, auth, marketing, and persistent toasts passed
under Node. The initial Bun-hosted Vitest attempt failed to apply mocks correctly;
no source changes were needed for the Node run. `just check` passed, and the final
UI changes also passed the web TypeScript check and desktop/mobile browser checks.
The Lighthouse measurements below are from September 24 and were not rerun.

Launch remains blocked on resolving the PR's conflicts with current `main`,
obtaining CI/review for the merged result, and publishing/verifying `dist-site`
through the website origin. The app deployment still publishes only `dist`.
Mobile performance and actual CDN/auth-routing checks remain outstanding as
described below. No production cutover was performed.

## URLs and crawlability

- All 30 live sitemap pages return 200 on production and keep the same path in
  the candidate. No indexed page was moved, so no new migration redirects are
  required. The route inventory remains in `migration-baseline.json`.
- Compared 1,269 internal link references from live and candidate HTML,
  including 50 fragment references; no missing static page or fragment targets.
  All 743 referenced local assets exist. App destinations are checked separately
  because they belong to the app origin, not the marketing artifact.
- All baseline pages remain reachable from homepage HTML. Titles (except the
  intentionally redesigned homepage), canonical URLs, unique descriptions,
  social cards, language, headings, and substantive non-JavaScript content pass.
- JSON-LD parses, all baseline schema types remain, and FAQ questions match
  visible page content. Blog authors, dates, and comparison content remain.
- Public pages stay indexable; signup pages remain outside the sitemap with
  `noindex, follow`. Production robots allows public pages and excludes app/test.
- The production artifact serves true 404s in the local audit server. The
  existing edge must also preserve that behavior; a static test server cannot
  prove CDN rewrites or WAF behavior.

## Live redirects and older URLs

Observed without cookies or JavaScript, with redirects not automatically followed:

| URL | Live result | Required treatment |
| --- | --- | --- |
| `http://macro.com/documents?utm_source=seo-recheck` | 301 to HTTPS, query retained | Preserve HTTPS redirect |
| `https://www.macro.com/documents?utm_source=seo-recheck` | 302 to apex, query retained | Preserve path/query; use a permanent redirect for any deliberate future URL move |
| `/email/`, `/terms/`, `/pricing/` | 200 | Preserve trailing-slash directory indexes and slashless canonical |
| `/app` | 302 to `/app/` | Preserve app routing |
| `/app/login`, `/app/calendar` | 200 | Preserve app deep links |
| `https://chat.macro.com/` | 200 at HTTP layer | Preserve separate chat-host behavior; this was not an authenticated routing test |
| `/resources`, `/resources/`, `/resources/sitemap.xml` | 404 | Keep the existing Ghost proxy for any historical resource URLs it still serves |
| `/macros/`, `/features/pdf`, two old `/blog/*` URLs surfaced by search | 403 | Existing live failures, not URLs removed by this branch |
| `/llms.txt`, `/llms-full.txt`, `/agents.md` | 403 | No working AI text files were lost |
| `/.well-known/openid-configuration` | 403 | Does not establish whether other OIDC paths work; retain the existing edge behavior |
| An unknown public path | 403 | Candidate should serve 404/410, never homepage HTML with 200 |

The sampled old blog URLs were `/blog/how-to-convert-pdf-to-csv-on-mac/` and
`/blog/how-to-insert-pdf-into-powerpoint/`. Search still surfaces these older
paths, but direct live requests fail. Restoring that historical content or
mapping it to genuinely equivalent replacements is a separate existing-site
cleanup; do not invent redirects to unrelated pages.

## Regressions repaired in this pass

- The initial-JavaScript gate caught **449,898 bytes gzip**, above its existing
  400,000-byte budget. The hidden Full Demo sidebar still imported app UI, and
  several small helpers imported the entire UI barrel. The sidebar now loads
  only if the demo is enabled/opened, and those helpers import their owners
  directly. No app behavior changed.
- Idle preloading was fetching all interactive demos even when visitors stayed
  at the hero. Preloading now starts within 2,400px of the scroll viewport;
  editors still mount within 1,400px. Save-Data/2G exclusions remain.
- Large prerendered theme attributes/styles could push charset too far into
  HTML. Theme tokens now stay in the head and charset stays within the first
  1,024 bytes. First-paint styling and runtime theme overrides remain.
- Added a homepage main landmark and an empty alt on the decorative tracking
  pixel. Expanded the build gate to check actual link/fragment destinations,
  referenced local assets, and charset placement.

## Performance and browser verification

The fixed build passes its 400,000-byte initial-JS budget at **393,806 bytes
gzip** (12.5% below the failure found during this recheck). Homepage HTML is
50,316 bytes gzip. A cold mobile browser stayed at the hero for four seconds
without requesting any editor/demo chunks or Loro WASM.

Lighthouse 13.5.0, Chromium 147, cold navigation, default simulated mobile
throttling. The candidate is the production artifact served locally with gzip
and immutable hashed assets; live is the real CDN. Analytics were enabled.
These are single-run diagnostic measurements, not field Core Web Vitals or a
controlled proof of deployed improvement. The intermediate run already had
the first hidden-sidebar fix, but still eagerly preloaded distant demos.

| Metric | Live macro.com | Candidate, intermediate | Candidate, fixed |
| --- | --- | --- | --- |
| Performance score | 39 | 35 | 52 |
| First contentful paint | 1.8s | 4.9s | 4.8s |
| Largest contentful paint | 11.5s | 8.5s | 5.1s |
| Total blocking time | 3,230ms | 2,230ms | 670ms |
| Cumulative layout shift | 0 | 0 | 0 |
| Total navigation payload | 5,131 KiB | 6,242 KiB | 1,214 KiB |
| SEO score | 100 | 100 | 100 |
| Accessibility score | 80 | 98 | 100 |

The fixed candidate's desktop Lighthouse run scored **94**, with **1.0s FCP**,
**1.4s LCP**, **70ms TBT**, and **0 CLS** using the desktop preset.

**Mobile performance remains a follow-up, not a clean pass.** First paint is
slower than live in this test. The shared app stylesheet is about 67 KB gzip
and render-blocking; analytics also contribute substantial main-thread work.
Further isolate critical homepage styling and defer nonessential analytics
work without losing attribution, then measure the actual deployed candidate.
The lab scores do not certify accessibility or validate every rich-result rule.

Validation completed:

- Fresh `VITE_APP_BASE_URL=https://macro.com bun run build:site` and the expanded
  SEO build gate: pass; 645 candidate internal links and 716 asset references
  checked by the permanent gate, plus the broader live/candidate crawl above.
- `bun run check:seo:live http://127.0.0.1:3993`: all 30 routes, crawler files,
  signup noindex, and unknown-route status pass.
- Web and marketing TypeScript checks, `just check`, and `git diff --check`:
  pass. Existing informational lint/rule warnings remain.
- Browser checks on `/`, `/email`, `/documents`, `/pricing`, two blog pages,
  and `/start`: no runtime exceptions or missing local requests; canonical and
  heading survive client startup. Menu opens/closes and the document/timeline
  demo loads on scroll. The disabled Full Demo remains unmounted.
- Homepage without JavaScript: readable styled headline, five feature sections,
  and 53 links. Desktop and 390px/320px mobile checks: no horizontal overflow.
- All 11 articles preserve live headline, author, publication/modification date,
  canonical URL, and main-entity metadata. No referenced image lacks an alt
  attribute. No unresolved Git LFS pointers found in the generated artifact.

## Deployment checks still required

No CDN, DNS, WAF, production deployment, or Search Console settings were changed.
The current app deployment still does not publish `dist-site` automatically.
Follow `MIGRATION.md` before switching origins: preserve app/Ghost/OIDC routing,
redirects, cache policy and existing immutable assets, then run
`bun run check:seo:live https://candidate-host` against the actual edge.

Real Googlebot/Bingbot access, Search Console indexing/canonicals, backlinks
outside the crawled inventory, historical traffic, and field Core Web Vitals
were not available to this local audit. Recheck those at cutover. In particular,
lab total blocking time is not a measurement of real-user INP.

For AEO, crawlable text, useful feature/comparison pages, and accurate structured
data remain intact. Google requires no special AI text file or extra schema for
AI Overviews/AI Mode; satisfying the technical requirements does not guarantee
indexing or citations. [Google's current guidance](https://developers.google.com/search/docs/appearance/ai-features).

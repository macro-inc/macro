/// HTTP client for the bundle update API.
pub mod api_client;
/// Filesystem operations for bundle extraction and persistence.
pub mod fs;
/// System information queries (version, arch, target, cache dir).
pub mod system_info;

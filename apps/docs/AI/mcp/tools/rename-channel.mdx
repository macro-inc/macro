---
title: RenameChannel
description: "Rename an existing channel. Requires the current user to be an active channel participant. Direct-message channels cannot be renamed. Use only when the user asks to rename a channel."
---

# RenameChannel

Rename an existing channel. Requires the current user to be an active channel participant. Direct-message channels cannot be renamed. Use only when the user asks to rename a channel.

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `channelId` | string | Yes | Channel id to rename. |
| `name` | string | Yes | New display name for the channel. |

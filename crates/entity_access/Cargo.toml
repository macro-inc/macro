[package]
edition = "2024"
name = "entity_access"
publish = false
version = "0.1.0"

[[bin]]
name = "explain_access"
path = "src/bin/explain_access.rs"
required-features = ["explain_binary"]

[features]
axum = [
  "dep:axum",
  "dep:macro_authorization",
  "dep:model",
  "dep:model-error-response",
]
default = ["axum", "inbound", "outbound", "ports"]
explain_binary = [
  "dep:clap",
  "dep:database_env_vars",
  "dep:macro_entrypoint",
  "outbound",
  "ports",
]
inbound = ["axum", "ports"]
outbound = ["dep:cached", "ports"]
ports = ["dep:tokio"]
schema = ["dep:utoipa"]

[dependencies]
# Core
anyhow = { workspace = true }
futures = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }

# Domain models (always included)
bot_id = { path = "../bot_id" }
rootcause = { workspace = true }
macro_user_id = { path = "../macro_user_id" }
model-entity = { path = "../model-entity" }
models_entity_access_management = { path = "../models_entity_access_management" }
models_permissions = { path = "../models_permissions" }

clap = { workspace = true, optional = true }
database_env_vars = { path = "../database_env_vars", optional = true }
macro_entrypoint = { path = "../macro_entrypoint", optional = true }

# Async runtime (for ports)
tokio = { workspace = true, optional = true }

# Axum dependencies (for inbound)
axum = { workspace = true, optional = true }
macro_authorization = { path = "../macro_authorization", optional = true, default-features = false, features = ["axum"] }
model = { path = "../model", optional = true }
model-error-response = { path = "../model-error-response", optional = true }

# OpenAPI schema (optional)
utoipa = { workspace = true, optional = true }

# Database
cached = { workspace = true, optional = true }
sqlx = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[dev-dependencies]
cool_asserts = "2"
macro_db_migrator = { path = "../macro_db_migrator" }
mockall = { workspace = true }
model_user = { path = "../model_user" }
rootcause = { workspace = true }
sqlx = { workspace = true, features = ["postgres", "runtime-tokio-rustls"] }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
tower = { workspace = true }

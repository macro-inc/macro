[package]
edition = "2024"
name = "webhook_signature"
publish = false
version = "0.1.0"

[dependencies]
hex = { workspace = true }
hmac = { workspace = true }
sha2 = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

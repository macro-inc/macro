//! Persona domain: models, ports, and the service that owns persona policy.

/// Domain error.
pub mod error;
/// Domain models.
pub mod models;
/// Ports exposed to and required by the domain.
pub mod ports;
/// Domain service.
pub mod service;

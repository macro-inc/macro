[package]
edition = "2024"
name = "models_opensearch"
publish = false
version = "0.1.0"

[dependencies]
serde = { workspace = true }
strum = { workspace = true }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

[package]
edition = "2024"
name = "lexical_mention_extractor"
publish = false
version = "0.1.0"

[dependencies]
messages = { path = "../messages", features = ["ports"] }
rootcause = { workspace = true }
lexical_client = { path = "../lexical_client" }
workspace-hack = { version = "0.1", path = "../workspace-hack" }

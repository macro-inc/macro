# DO NOT EDIT — regenerate with `cargo x workflows` (from the repository root).
# Source: tooling/xtask/crates/xtask_workflows/src/workflows/deploy_fusionauth_instance.rs
name: Deploy FusionAuth Instance
on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        required: true
        default: dev
        options:
        - dev
        - prod
        description: The environment to deploy to.
jobs:
  deploy:
    name: Deploy fusionauth-instance
    uses: ./.github/workflows/reusable_deploy_service.yml
    with:
      environment: ${{ inputs.environment }}
      service-name: fusionauth-instance
      pulumi-stack-name: fusionauth-instance
      use-docker: false
    secrets: inherit
concurrency:
  group: deploy-fusionauth-instance-${{ inputs.environment }}
  cancel-in-progress: false

# DO NOT EDIT — regenerate with `cargo x workflows` (from the repository root).
# Source: tooling/xtask/crates/xtask_workflows/src/workflows/build_agent_daemon_on_tag.rs
name: Build Agent Daemon on Tag
'on':
  push:
    tags:
    - v[0-9]*
  workflow_dispatch:
    inputs:
      ref:
        description: Optional release tag override (v* or refs/tags/v*). Leave empty to build the branch or tag selected for this workflow run.
        type: string
jobs:
  resolve-ref:
    name: Resolve build ref
    runs-on: namespace-profile-linux-tiny-no-cache
    outputs:
      ref: ${{ steps.resolve.outputs.ref }}
    steps:
    - id: resolve
      name: Resolve ref
      run: |
        set -euo pipefail

        # Resolve the git ref for desktop builds from any trigger type:
        #   push (tag)          → github.ref
        #   create (tag)        → github.event.ref
        #   workflow_dispatch   → inputs.ref (release tag) or the selected workflow ref

        case "$EVENT_NAME" in
          push)
            echo "ref=$SELECTED_REF" >> "$GITHUB_OUTPUT"
            ;;
          create)
            if [ "$GITHUB_EVENT_REF_TYPE" != "tag" ]; then
              echo "create event was not a tag; skipping" >&2
              exit 1
            fi
            echo "ref=$GITHUB_EVENT_REF" >> "$GITHUB_OUTPUT"
            ;;
          workflow_dispatch)
            # An empty override means "build the ref selected in GitHub's Run workflow
            # menu". This allows branch builds while keeping explicit overrides limited
            # to release tags.
            if [ -z "$INPUT_REF" ]; then
              echo "ref=$SELECTED_REF" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            if [[ "$INPUT_REF" =~ ^refs/tags/v[0-9][0-9A-Za-z._-]*$ ]]; then
              echo "ref=$INPUT_REF" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            if [[ "$INPUT_REF" =~ ^v[0-9][0-9A-Za-z._-]*$ ]]; then
              echo "ref=refs/tags/$INPUT_REF" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "The ref input only accepts release tags (v*). To build a branch, select it in the Run workflow menu and leave ref empty." >&2
            exit 1
            ;;
          *)
            echo "Unexpected event: $EVENT_NAME" >&2
            exit 1
            ;;
        esac
      shell: bash
      env:
        EVENT_NAME: ${{ github.event_name }}
        INPUT_REF: ${{ inputs.ref }}
        GITHUB_EVENT_REF: ${{ github.event.ref }}
        GITHUB_EVENT_REF_TYPE: ${{ github.event.ref_type }}
        SELECTED_REF: ${{ github.ref }}
  build-daemon-linux-x86-64:
    needs:
    - resolve-ref
    name: Build daemon (linux-x86_64)
    runs-on: namespace-profile-linux-rust-ci
    steps:
    - name: Checkout Repo
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      with:
        ref: ${{ needs.resolve-ref.outputs.ref }}
        persist-credentials: 'false'
    - name: Mount Namespace cache volume
      uses: namespacelabs/nscloud-cache-action@15799a6b54e5765f85b2aac25b3f0df43ed571c0
      with:
        cache: rust
        path: /nix
      continue-on-error: true
    - name: Setup Nix
      uses: './.github/actions/setup-nix'
      with:
        nix-cache-url: ${{ vars.NIX_CACHE_URL }}
        nix-cache-public-key: ${{ vars.NIX_CACHE_PUBLIC_KEY }}
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    - name: Setup Nix dev shell
      uses: './.github/actions/setup-nix-dev-shell'
      with:
        shell: agent-daemon
    - name: Configure Namespace remote sccache
      if: (github.event_name != 'pull_request' && github.event_name != 'pull_request_target') || github.event.pull_request.head.repo.full_name == github.repository
      run: |-
        set -euo pipefail
        env_file="$(mktemp "$RUNNER_TEMP/namespace-sccache.XXXXXX")"
        trap 'rm -f "$env_file"' EXIT
        if nsc cache sccache setup --cache_name sccache-ci > "$env_file"; then
          # Register credential values as masked BEFORE exporting: GitHub only
          # masks `secrets.*`, so without this SCCACHE_WEBDAV_TOKEN — a broad,
          # ~24h Namespace workspace token (registry + cache write) — printed
          # verbatim in every subsequent step's env dump. Masking is selective by
          # key name: masking non-secrets like the endpoint URL or key prefix
          # would redact those strings everywhere in the logs.
          while IFS= read -r line; do
            k="${line%%=*}"
            v="${line#*=}"
            [ -n "$v" ] && [ "$k" != "$line" ] || continue
            case "$k" in
              *TOKEN*|*SECRET*|*PASSWORD*) echo "::add-mask::$v" ;;
            esac
          done < "$env_file"
          cat "$env_file" >> "$GITHUB_ENV"
          # Force the next compiler invocation to start a server with the new remote
          # backend even if a setup hook happened to launch one already.
          sccache --stop-server >/dev/null 2>&1 || true
        else
          echo "::warning::Namespace remote sccache setup failed; using local cache fallback"
        fi
    - id: metadata
      name: Derive artifact metadata
      run: |
        set -euo pipefail
        tag="${RAW_REF#refs/tags/}"
        if [ -z "$tag" ]; then
          tag="${GITHUB_REF_NAME:-untagged}"
        fi
        safe_tag=$(printf '%s' "$tag" | sed 's#[/\\:*?"<>|]#-#g' | tr -d '\r\n')
        echo "tag=$tag" >> "$GITHUB_OUTPUT"
        echo "safe_tag=$safe_tag" >> "$GITHUB_OUTPUT"
      shell: bash
      env:
        RAW_REF: ${{ needs.resolve-ref.outputs.ref }}
    - name: Build daemon
      run: |
        set -euo pipefail

        # Statically linked musl build of the self-hosted coding-agent daemon.
        #
        # The musl std that ships with the toolchain links pure-Rust crates on its own,
        # but aws-lc-sys — rustls' crypto provider, and so the whole reason the daemon
        # can talk HTTPS and WSS — is C, and needs a compiler that targets musl. zig is
        # that compiler, which is what cargo-zigbuild wires up; it also makes the
        # aarch64 build a cross-compile rather than a second runner.
        #
        # aws-lc-sys' own cc builder rejects zig, so force its cmake builder instead.
        # The Lambda builds hit the same wall and take the same way out
        # (see nix/cloud-storage.nix).
        export AWS_LC_SYS_CMAKE_BUILDER=1

        # The daemon runs no queries of its own, but crates in its dependency graph
        # carry sqlx macros; offline mode keeps a release build from wanting a database.
        export SQLX_OFFLINE=true

        cargo zigbuild --release --package coding_agent_worker --target "$TARGET"
      shell: bash
      env:
        TARGET: x86_64-unknown-linux-musl
    - name: Package daemon
      run: |
        set -euo pipefail

        # Package one built daemon binary as a release asset — and prove it is as
        # self-contained as its platform allows before it ships. A binary that resolves
        # a library at startup on the build runner and not on a user's machine is
        # exactly the failure this whole pipeline exists to prevent, so it is checked
        # here rather than assumed.

        binary="target/$TARGET/release/macrod"
        if [ ! -x "$binary" ]; then
          echo "no daemon binary at $binary" >&2
          exit 1
        fi

        case "$TARGET" in
          *-linux-musl)
            # Statically linked: nothing may be left to resolve at run time. `ldd`
            # exits non-zero with "not a dynamic executable" for a static binary, so
            # a successful ldd that names shared objects is the failure case.
            if ldd "$binary" 2>/dev/null | grep -q '=>'; then
              echo "expected a static binary, but $binary still links:" >&2
              ldd "$binary" >&2
              exit 1
            fi
            ;;
          *-apple-darwin)
            # macOS has no static libSystem, so "self-contained" means nothing from
            # outside the OS: a Homebrew or Nix-store path is one that will not exist
            # on the machine that downloads this.
            strays=$(otool -L "$binary" | tail -n +2 | awk '{ print $1 }' \
              | grep -Ev '^(/usr/lib/|/System/Library/)' || true)
            if [ -n "$strays" ]; then
              echo "daemon links libraries that will not exist on a user's Mac:" >&2
              echo "$strays" >&2
              exit 1
            fi
            ;;
          *)
            echo "no self-containment check defined for $TARGET" >&2
            exit 1
            ;;
        esac

        # It has to actually run, too — a binary that links cleanly and dies on its
        # first instruction still ships broken. `--version` is the cheapest end-to-end
        # proof, and the caller says whether this runner can execute this slice: the
        # cross-built ones (aarch64 anywhere, x86_64 on an Apple Silicon runner) cannot.
        if [ "$SMOKE_RUN" = "1" ]; then
          "$binary" --version
        else
          echo "skipping smoke run: $TARGET does not execute on this runner"
        fi

        mkdir -p artifacts
        name="macrod-${SAFE_TAG}-${SLUG}"
        stage=$(mktemp -d)
        install -m 755 "$binary" "$stage/macrod"
        tar -czf "artifacts/${name}.tar.gz" -C "$stage" macrod
        rm -rf "$stage"

        # coreutils on Linux, BSD/perl on macOS.
        (
          cd artifacts
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${name}.tar.gz" >"${name}.tar.gz.sha256"
          else
            shasum -a 256 "${name}.tar.gz" >"${name}.tar.gz.sha256"
          fi
        )

        echo "packaged ${name}.tar.gz"
      shell: bash
      env:
        TARGET: x86_64-unknown-linux-musl
        SLUG: linux-x86_64
        SAFE_TAG: ${{ steps.metadata.outputs.safe_tag }}
        SMOKE_RUN: '1'
    - name: Upload macrod-linux-x86_64-${{ steps.metadata.outputs.safe_tag }} artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: macrod-linux-x86_64-${{ steps.metadata.outputs.safe_tag }}
        path: artifacts/*
        if-no-files-found: error
        retention-days: '30'
    - name: Teardown Nix
      if: always()
      uses: './.github/actions/teardown-nix'
  build-daemon-linux-aarch64:
    needs:
    - resolve-ref
    name: Build daemon (linux-aarch64)
    runs-on: namespace-profile-linux-rust-ci
    steps:
    - name: Checkout Repo
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      with:
        ref: ${{ needs.resolve-ref.outputs.ref }}
        persist-credentials: 'false'
    - name: Mount Namespace cache volume
      uses: namespacelabs/nscloud-cache-action@15799a6b54e5765f85b2aac25b3f0df43ed571c0
      with:
        cache: rust
        path: /nix
      continue-on-error: true
    - name: Setup Nix
      uses: './.github/actions/setup-nix'
      with:
        nix-cache-url: ${{ vars.NIX_CACHE_URL }}
        nix-cache-public-key: ${{ vars.NIX_CACHE_PUBLIC_KEY }}
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    - name: Setup Nix dev shell
      uses: './.github/actions/setup-nix-dev-shell'
      with:
        shell: agent-daemon
    - name: Configure Namespace remote sccache
      if: (github.event_name != 'pull_request' && github.event_name != 'pull_request_target') || github.event.pull_request.head.repo.full_name == github.repository
      run: |-
        set -euo pipefail
        env_file="$(mktemp "$RUNNER_TEMP/namespace-sccache.XXXXXX")"
        trap 'rm -f "$env_file"' EXIT
        if nsc cache sccache setup --cache_name sccache-ci > "$env_file"; then
          # Register credential values as masked BEFORE exporting: GitHub only
          # masks `secrets.*`, so without this SCCACHE_WEBDAV_TOKEN — a broad,
          # ~24h Namespace workspace token (registry + cache write) — printed
          # verbatim in every subsequent step's env dump. Masking is selective by
          # key name: masking non-secrets like the endpoint URL or key prefix
          # would redact those strings everywhere in the logs.
          while IFS= read -r line; do
            k="${line%%=*}"
            v="${line#*=}"
            [ -n "$v" ] && [ "$k" != "$line" ] || continue
            case "$k" in
              *TOKEN*|*SECRET*|*PASSWORD*) echo "::add-mask::$v" ;;
            esac
          done < "$env_file"
          cat "$env_file" >> "$GITHUB_ENV"
          # Force the next compiler invocation to start a server with the new remote
          # backend even if a setup hook happened to launch one already.
          sccache --stop-server >/dev/null 2>&1 || true
        else
          echo "::warning::Namespace remote sccache setup failed; using local cache fallback"
        fi
    - id: metadata
      name: Derive artifact metadata
      run: |
        set -euo pipefail
        tag="${RAW_REF#refs/tags/}"
        if [ -z "$tag" ]; then
          tag="${GITHUB_REF_NAME:-untagged}"
        fi
        safe_tag=$(printf '%s' "$tag" | sed 's#[/\\:*?"<>|]#-#g' | tr -d '\r\n')
        echo "tag=$tag" >> "$GITHUB_OUTPUT"
        echo "safe_tag=$safe_tag" >> "$GITHUB_OUTPUT"
      shell: bash
      env:
        RAW_REF: ${{ needs.resolve-ref.outputs.ref }}
    - name: Build daemon
      run: |
        set -euo pipefail

        # Statically linked musl build of the self-hosted coding-agent daemon.
        #
        # The musl std that ships with the toolchain links pure-Rust crates on its own,
        # but aws-lc-sys — rustls' crypto provider, and so the whole reason the daemon
        # can talk HTTPS and WSS — is C, and needs a compiler that targets musl. zig is
        # that compiler, which is what cargo-zigbuild wires up; it also makes the
        # aarch64 build a cross-compile rather than a second runner.
        #
        # aws-lc-sys' own cc builder rejects zig, so force its cmake builder instead.
        # The Lambda builds hit the same wall and take the same way out
        # (see nix/cloud-storage.nix).
        export AWS_LC_SYS_CMAKE_BUILDER=1

        # The daemon runs no queries of its own, but crates in its dependency graph
        # carry sqlx macros; offline mode keeps a release build from wanting a database.
        export SQLX_OFFLINE=true

        cargo zigbuild --release --package coding_agent_worker --target "$TARGET"
      shell: bash
      env:
        TARGET: aarch64-unknown-linux-musl
    - name: Package daemon
      run: |
        set -euo pipefail

        # Package one built daemon binary as a release asset — and prove it is as
        # self-contained as its platform allows before it ships. A binary that resolves
        # a library at startup on the build runner and not on a user's machine is
        # exactly the failure this whole pipeline exists to prevent, so it is checked
        # here rather than assumed.

        binary="target/$TARGET/release/macrod"
        if [ ! -x "$binary" ]; then
          echo "no daemon binary at $binary" >&2
          exit 1
        fi

        case "$TARGET" in
          *-linux-musl)
            # Statically linked: nothing may be left to resolve at run time. `ldd`
            # exits non-zero with "not a dynamic executable" for a static binary, so
            # a successful ldd that names shared objects is the failure case.
            if ldd "$binary" 2>/dev/null | grep -q '=>'; then
              echo "expected a static binary, but $binary still links:" >&2
              ldd "$binary" >&2
              exit 1
            fi
            ;;
          *-apple-darwin)
            # macOS has no static libSystem, so "self-contained" means nothing from
            # outside the OS: a Homebrew or Nix-store path is one that will not exist
            # on the machine that downloads this.
            strays=$(otool -L "$binary" | tail -n +2 | awk '{ print $1 }' \
              | grep -Ev '^(/usr/lib/|/System/Library/)' || true)
            if [ -n "$strays" ]; then
              echo "daemon links libraries that will not exist on a user's Mac:" >&2
              echo "$strays" >&2
              exit 1
            fi
            ;;
          *)
            echo "no self-containment check defined for $TARGET" >&2
            exit 1
            ;;
        esac

        # It has to actually run, too — a binary that links cleanly and dies on its
        # first instruction still ships broken. `--version` is the cheapest end-to-end
        # proof, and the caller says whether this runner can execute this slice: the
        # cross-built ones (aarch64 anywhere, x86_64 on an Apple Silicon runner) cannot.
        if [ "$SMOKE_RUN" = "1" ]; then
          "$binary" --version
        else
          echo "skipping smoke run: $TARGET does not execute on this runner"
        fi

        mkdir -p artifacts
        name="macrod-${SAFE_TAG}-${SLUG}"
        stage=$(mktemp -d)
        install -m 755 "$binary" "$stage/macrod"
        tar -czf "artifacts/${name}.tar.gz" -C "$stage" macrod
        rm -rf "$stage"

        # coreutils on Linux, BSD/perl on macOS.
        (
          cd artifacts
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${name}.tar.gz" >"${name}.tar.gz.sha256"
          else
            shasum -a 256 "${name}.tar.gz" >"${name}.tar.gz.sha256"
          fi
        )

        echo "packaged ${name}.tar.gz"
      shell: bash
      env:
        TARGET: aarch64-unknown-linux-musl
        SLUG: linux-aarch64
        SAFE_TAG: ${{ steps.metadata.outputs.safe_tag }}
        SMOKE_RUN: '0'
    - name: Upload macrod-linux-aarch64-${{ steps.metadata.outputs.safe_tag }} artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: macrod-linux-aarch64-${{ steps.metadata.outputs.safe_tag }}
        path: artifacts/*
        if-no-files-found: error
        retention-days: '30'
    - name: Teardown Nix
      if: always()
      uses: './.github/actions/teardown-nix'
  build-daemon-macos-aarch64:
    needs:
    - resolve-ref
    name: Build daemon (macos-aarch64)
    runs-on: nscloud-macos-sequoia-arm64-6x14
    steps:
    - name: Checkout Repo
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      with:
        ref: ${{ needs.resolve-ref.outputs.ref }}
        persist-credentials: 'false'
    - name: Setup Rust
      uses: './.github/actions/setup-rust'
      with:
        sccache: 'false'
        rust-cache: 'false'
    - id: metadata
      name: Derive artifact metadata
      run: |
        set -euo pipefail
        tag="${RAW_REF#refs/tags/}"
        if [ -z "$tag" ]; then
          tag="${GITHUB_REF_NAME:-untagged}"
        fi
        safe_tag=$(printf '%s' "$tag" | sed 's#[/\\:*?"<>|]#-#g' | tr -d '\r\n')
        echo "tag=$tag" >> "$GITHUB_OUTPUT"
        echo "safe_tag=$safe_tag" >> "$GITHUB_OUTPUT"
      shell: bash
      env:
        RAW_REF: ${{ needs.resolve-ref.outputs.ref }}
    - name: Build daemon
      run: |
        set -euo pipefail

        # macOS build of the self-hosted coding-agent daemon.
        #
        # No zig here: the Apple targets link against the SDK the runner already has,
        # and the arm64 runner cross-compiles the x86_64 slice on its own. All this
        # step needs is the matching rust-std.
        #
        # See build_agent_daemon_linux.sh for why sqlx runs offline.
        export SQLX_OFFLINE=true

        rustup target add "$TARGET"

        # aws-lc-sys compiles its C through cmake. The GitHub macOS image ships cmake
        # today, but the build is too far downstream to discover that it stopped.
        if ! command -v cmake >/dev/null 2>&1; then
          brew install cmake
        fi

        cargo build --release --package coding_agent_worker --target "$TARGET"
      shell: bash
      env:
        TARGET: aarch64-apple-darwin
    - name: Package daemon
      run: |
        set -euo pipefail

        # Package one built daemon binary as a release asset — and prove it is as
        # self-contained as its platform allows before it ships. A binary that resolves
        # a library at startup on the build runner and not on a user's machine is
        # exactly the failure this whole pipeline exists to prevent, so it is checked
        # here rather than assumed.

        binary="target/$TARGET/release/macrod"
        if [ ! -x "$binary" ]; then
          echo "no daemon binary at $binary" >&2
          exit 1
        fi

        case "$TARGET" in
          *-linux-musl)
            # Statically linked: nothing may be left to resolve at run time. `ldd`
            # exits non-zero with "not a dynamic executable" for a static binary, so
            # a successful ldd that names shared objects is the failure case.
            if ldd "$binary" 2>/dev/null | grep -q '=>'; then
              echo "expected a static binary, but $binary still links:" >&2
              ldd "$binary" >&2
              exit 1
            fi
            ;;
          *-apple-darwin)
            # macOS has no static libSystem, so "self-contained" means nothing from
            # outside the OS: a Homebrew or Nix-store path is one that will not exist
            # on the machine that downloads this.
            strays=$(otool -L "$binary" | tail -n +2 | awk '{ print $1 }' \
              | grep -Ev '^(/usr/lib/|/System/Library/)' || true)
            if [ -n "$strays" ]; then
              echo "daemon links libraries that will not exist on a user's Mac:" >&2
              echo "$strays" >&2
              exit 1
            fi
            ;;
          *)
            echo "no self-containment check defined for $TARGET" >&2
            exit 1
            ;;
        esac

        # It has to actually run, too — a binary that links cleanly and dies on its
        # first instruction still ships broken. `--version` is the cheapest end-to-end
        # proof, and the caller says whether this runner can execute this slice: the
        # cross-built ones (aarch64 anywhere, x86_64 on an Apple Silicon runner) cannot.
        if [ "$SMOKE_RUN" = "1" ]; then
          "$binary" --version
        else
          echo "skipping smoke run: $TARGET does not execute on this runner"
        fi

        mkdir -p artifacts
        name="macrod-${SAFE_TAG}-${SLUG}"
        stage=$(mktemp -d)
        install -m 755 "$binary" "$stage/macrod"
        tar -czf "artifacts/${name}.tar.gz" -C "$stage" macrod
        rm -rf "$stage"

        # coreutils on Linux, BSD/perl on macOS.
        (
          cd artifacts
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${name}.tar.gz" >"${name}.tar.gz.sha256"
          else
            shasum -a 256 "${name}.tar.gz" >"${name}.tar.gz.sha256"
          fi
        )

        echo "packaged ${name}.tar.gz"
      shell: bash
      env:
        TARGET: aarch64-apple-darwin
        SLUG: macos-aarch64
        SAFE_TAG: ${{ steps.metadata.outputs.safe_tag }}
        SMOKE_RUN: '1'
    - name: Upload macrod-macos-aarch64-${{ steps.metadata.outputs.safe_tag }} artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: macrod-macos-aarch64-${{ steps.metadata.outputs.safe_tag }}
        path: artifacts/*
        if-no-files-found: error
        retention-days: '30'
  build-daemon-macos-x86-64:
    needs:
    - resolve-ref
    name: Build daemon (macos-x86_64)
    runs-on: nscloud-macos-sequoia-arm64-6x14
    steps:
    - name: Checkout Repo
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
      with:
        ref: ${{ needs.resolve-ref.outputs.ref }}
        persist-credentials: 'false'
    - name: Setup Rust
      uses: './.github/actions/setup-rust'
      with:
        sccache: 'false'
        rust-cache: 'false'
    - id: metadata
      name: Derive artifact metadata
      run: |
        set -euo pipefail
        tag="${RAW_REF#refs/tags/}"
        if [ -z "$tag" ]; then
          tag="${GITHUB_REF_NAME:-untagged}"
        fi
        safe_tag=$(printf '%s' "$tag" | sed 's#[/\\:*?"<>|]#-#g' | tr -d '\r\n')
        echo "tag=$tag" >> "$GITHUB_OUTPUT"
        echo "safe_tag=$safe_tag" >> "$GITHUB_OUTPUT"
      shell: bash
      env:
        RAW_REF: ${{ needs.resolve-ref.outputs.ref }}
    - name: Build daemon
      run: |
        set -euo pipefail

        # macOS build of the self-hosted coding-agent daemon.
        #
        # No zig here: the Apple targets link against the SDK the runner already has,
        # and the arm64 runner cross-compiles the x86_64 slice on its own. All this
        # step needs is the matching rust-std.
        #
        # See build_agent_daemon_linux.sh for why sqlx runs offline.
        export SQLX_OFFLINE=true

        rustup target add "$TARGET"

        # aws-lc-sys compiles its C through cmake. The GitHub macOS image ships cmake
        # today, but the build is too far downstream to discover that it stopped.
        if ! command -v cmake >/dev/null 2>&1; then
          brew install cmake
        fi

        cargo build --release --package coding_agent_worker --target "$TARGET"
      shell: bash
      env:
        TARGET: x86_64-apple-darwin
    - name: Package daemon
      run: |
        set -euo pipefail

        # Package one built daemon binary as a release asset — and prove it is as
        # self-contained as its platform allows before it ships. A binary that resolves
        # a library at startup on the build runner and not on a user's machine is
        # exactly the failure this whole pipeline exists to prevent, so it is checked
        # here rather than assumed.

        binary="target/$TARGET/release/macrod"
        if [ ! -x "$binary" ]; then
          echo "no daemon binary at $binary" >&2
          exit 1
        fi

        case "$TARGET" in
          *-linux-musl)
            # Statically linked: nothing may be left to resolve at run time. `ldd`
            # exits non-zero with "not a dynamic executable" for a static binary, so
            # a successful ldd that names shared objects is the failure case.
            if ldd "$binary" 2>/dev/null | grep -q '=>'; then
              echo "expected a static binary, but $binary still links:" >&2
              ldd "$binary" >&2
              exit 1
            fi
            ;;
          *-apple-darwin)
            # macOS has no static libSystem, so "self-contained" means nothing from
            # outside the OS: a Homebrew or Nix-store path is one that will not exist
            # on the machine that downloads this.
            strays=$(otool -L "$binary" | tail -n +2 | awk '{ print $1 }' \
              | grep -Ev '^(/usr/lib/|/System/Library/)' || true)
            if [ -n "$strays" ]; then
              echo "daemon links libraries that will not exist on a user's Mac:" >&2
              echo "$strays" >&2
              exit 1
            fi
            ;;
          *)
            echo "no self-containment check defined for $TARGET" >&2
            exit 1
            ;;
        esac

        # It has to actually run, too — a binary that links cleanly and dies on its
        # first instruction still ships broken. `--version` is the cheapest end-to-end
        # proof, and the caller says whether this runner can execute this slice: the
        # cross-built ones (aarch64 anywhere, x86_64 on an Apple Silicon runner) cannot.
        if [ "$SMOKE_RUN" = "1" ]; then
          "$binary" --version
        else
          echo "skipping smoke run: $TARGET does not execute on this runner"
        fi

        mkdir -p artifacts
        name="macrod-${SAFE_TAG}-${SLUG}"
        stage=$(mktemp -d)
        install -m 755 "$binary" "$stage/macrod"
        tar -czf "artifacts/${name}.tar.gz" -C "$stage" macrod
        rm -rf "$stage"

        # coreutils on Linux, BSD/perl on macOS.
        (
          cd artifacts
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${name}.tar.gz" >"${name}.tar.gz.sha256"
          else
            shasum -a 256 "${name}.tar.gz" >"${name}.tar.gz.sha256"
          fi
        )

        echo "packaged ${name}.tar.gz"
      shell: bash
      env:
        TARGET: x86_64-apple-darwin
        SLUG: macos-x86_64
        SAFE_TAG: ${{ steps.metadata.outputs.safe_tag }}
        SMOKE_RUN: '0'
    - name: Upload macrod-macos-x86_64-${{ steps.metadata.outputs.safe_tag }} artifact
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: macrod-macos-x86_64-${{ steps.metadata.outputs.safe_tag }}
        path: artifacts/*
        if-no-files-found: error
        retention-days: '30'
  publish-daemon:
    needs:
    - resolve-ref
    - build-daemon-linux-x86-64
    - build-daemon-linux-aarch64
    - build-daemon-macos-aarch64
    - build-daemon-macos-x86-64
    if: '!cancelled() && needs.resolve-ref.result == ''success'' && (needs.build-daemon-linux-x86-64.result == ''success'' || needs.build-daemon-linux-aarch64.result == ''success'' || needs.build-daemon-macos-aarch64.result == ''success'' || needs.build-daemon-macos-x86-64.result == ''success'')'
    name: Upload Release Artifacts
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - id: metadata
      name: Derive artifact metadata
      run: |
        set -euo pipefail
        tag="${RAW_REF#refs/tags/}"
        if [ -z "$tag" ]; then
          tag="${GITHUB_REF_NAME:-untagged}"
        fi
        safe_tag=$(printf '%s' "$tag" | sed 's#[/\\:*?"<>|]#-#g' | tr -d '\r\n')
        echo "tag=$tag" >> "$GITHUB_OUTPUT"
        echo "safe_tag=$safe_tag" >> "$GITHUB_OUTPUT"
      shell: bash
      env:
        RAW_REF: ${{ needs.resolve-ref.outputs.ref }}
    - name: Download Build Artifacts
      uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0
      with:
        path: release-artifacts
        merge-multiple: 'true'
    - name: Upload Release Artifacts
      if: startsWith(steps.metadata.outputs.tag, 'v')
      uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65
      with:
        tag_name: ${{ steps.metadata.outputs.tag }}
        files: release-artifacts/*
        fail_on_unmatched_files: 'true'
concurrency:
  group: agent-daemon-${{ inputs.ref || github.ref_name }}
  cancel-in-progress: true

# DO NOT EDIT — regenerate with `cargo x workflows` (from the repository root).
# Source: tooling/xtask/crates/xtask_workflows/src/workflows/deploy_ai_editing_worker.rs
name: Deploy AI Editing Worker
on:
  push:
    branches:
    - main
    paths:
    - .github/workflows/deploy_ai_editing_worker.yml
    - bun.lock
    - package.json
    - services/ai-editing-worker/**
    - packages/collaboration/**
    - packages/lexical-core/**
    - packages/loro-mirror/**
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        description: The environment to deploy to. e.g. (dev, prod)
    secrets:
      CLOUDFLARE_API_TOKEN:
        required: true
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        default: dev
        options:
        - dev
        - prod
        description: The environment to deploy to
jobs:
  deploy:
    name: Deploy to Cloudflare
    runs-on: namespace-profile-linux-small
    steps:
    - name: Checkout
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      with:
        clean: 'false'
        persist-credentials: 'false'
    - name: Setup Bun
      uses: oven-sh/setup-bun@v2
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '22'
    - name: Install dependencies
      run: bun install --frozen-lockfile
    - name: Deploy with wrangler
      run: bun run deploy-${{ inputs.environment || 'dev' }}
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      working-directory: services/ai-editing-worker
concurrency:
  group: deploy-ai-editing-worker-${{ inputs.environment || 'dev' }}
  cancel-in-progress: false

[x] Multi Instance dispatcher
[x] Memory in prompt
  The memory crate needs to be included and used to do user memory.
[x] Enable / Disable crons
[x] Notifications
  Notify user on completion
[x] Remove cringe cron normalization
[x] Good UI
  [x] tab in agents
  [x] cron entity
  [x] view of stuff + history
[~] Put tool context building in `ai_tools`
  *cancer*
[x] Live Updates 
  [x] Show when running
    [x] Wire start / stop updates through connection gateway 
    [x] Track the block with connection gateway 
  [x] update infra
  [x] Update history 
    - When an automation _starts_ the chat_id is returned -> send with the start event via connection gateway
  [x] Show running indicator 
  [x] Prevent user from running automation multiple times at once

# punt
[] Stream agent 
  - Stream is the same as DCS
[] Fix noops in `ai_tools`
[] Make an infra file for tools that configures tools the same everywhere they're needed
[] Real unified list for history
[] Sharing / permissions
[] Mention menu
[] Delete schedules
  [] right click menu on unified list
  [] top bar
